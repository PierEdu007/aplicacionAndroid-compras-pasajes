name: Compilar APK Android Tunky Chasky

on:
  push:
    branches: [ main ]
    tags: [ 'v*' ]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    name: Build Android Standalone Release APK
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Clonar repositorio
        uses: actions/checkout@v4

      - name: ☕ Configurar Java 17 (JDK)
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: 🟢 Configurar Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: 📦 Instalar dependencias npm
        run: npm install --legacy-peer-deps

      - name: 🏗️ Generar proyecto nativo Android (Prebuild)
        run: npx expo prebuild --platform android --no-install

      - name: ⚙️ Configurar permisos de Gradle
        run: chmod +x android/gradlew

      - name: 🔨 Compilar APK Standalone Release (Empaquetado Offline)
        run: |
          cd android
          ./gradlew assembleRelease --no-daemon

      - name: 📦 Preparar APK para descarga
        run: |
          mkdir -p build-output
          cp android/app/build/outputs/apk/release/app-release.apk build-output/TunkyChaskyAdmin.apk || cp android/app/build/outputs/apk/release/*.apk build-output/TunkyChaskyAdmin.apk
          cp build-output/TunkyChaskyAdmin.apk build-output/TunkyChaskyAdmin-v1.2.0.apk

      - name: 📤 Subir APK como Artefacto de Descarga
        uses: actions/upload-artifact@v4
        with:
          name: TunkyChaskyAdmin-APK
          path: build-output/*.apk
          retention-days: 30

      - name: 🚀 Publicar Release con Enlace de Descarga Directa
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v1.2.0
          name: Tunky Chasky Admin v1.2.0 (Gestión de Ventas, Viajes Especiales y Facturación SUNAT)
          body: |
            ## 📱 Aplicación Móvil Oficial - Tunky Chasky Admin v1.2.0

            Aplicación oficial de gestión de ventas y control de pasajes para **Inversiones Tunki Chasky S.R.L.**:

            ### 📥 Descarga Directa del Instalador APK:
            - **[TunkyChaskyAdmin.apk](https://github.com/${{ github.repository }}/releases/download/v1.2.0/TunkyChaskyAdmin.apk)** (Última versión)
            - **[TunkyChaskyAdmin-v1.2.0.apk](https://github.com/${{ github.repository }}/releases/download/v1.2.0/TunkyChaskyAdmin-v1.2.0.apk)**

            ---

            ### ✨ Novedades y Mejoras v1.2.0:
            - **🚐 Modo Viaje Especial sin Asientos**: Emisión rápida de viajes especiales con origen y destino personalizados en mayúsculas.
            - **🧾 Emisión Directa SUNAT (NubeFact)**: Facturas y boletas electrónicas oficiales con validación de RUC y DNI del pasajero.
            - **🔍 Filtro Rápido de Viajes Especiales**: Pestaña dedicada en la pantalla de ventas para consultar viajes especiales al instante.
            - **📍 Rutas Kiteni Actualizadas**: Autoseed y selección de nuevas rutas para Kiteni, Quillabamba, Cusco y Calca.
            - **📅 Calendario Táctil y Selector de Horarios**: Selección visual rápida de fechas y horas en 1 toque.
            - **📧 Envío Automático de Comprobante por Correo**: Envío de boleta/factura en PDF y ticket de viaje al cliente.

            ---
            #### 📋 Instrucciones de Instalación:
            1. Descarga el archivo **`TunkyChaskyAdmin.apk`** en tu celular Android.
            2. Abre el archivo descargado.
            3. Si solicita permisos, selecciona **"Permitir instalar aplicaciones de esta fuente"**.
            4. Presiona **Instalar** y ¡listo!
          files: |
            build-output/TunkyChaskyAdmin.apk
            build-output/TunkyChaskyAdmin-v1.2.0.apk
          draft: false
          prerelease: false
          make_latest: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
